# G. Sterling Press

Permanent Android runtime for G. Sterling Press digital collections.

## Collection 001
**ABYSS — THE DESCENT**

The customer-facing V1 flow is built-in collection → **INSTALL THEME**. The legacy `.gstheme` importer is deprecated.

## Build
GitHub Actions compiles the Android debug APK on every push and exposes it as the `G_STERLING_PRESS_ONE_TAP_V1` artifact.

ABYSS visual masters are locked: runtime work must not redesign the approved artwork.
